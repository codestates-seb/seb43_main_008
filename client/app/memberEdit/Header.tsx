"use client";

import styled from "styled-components";

export default function Header() {
  return <HeaderContainer>ds</HeaderContainer>;
}

const HeaderContainer = styled.div`
  background-color: green;
`;
